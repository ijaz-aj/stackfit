# Normalise line endings to LF in the repo regardless of the checkout OS.
* text=auto eol=lf

# Binary / generated files git should not touch.
pnpm-lock.yaml -diff
*.png binary
*.pdf binary
*.docx binary
*.xlsx binary
