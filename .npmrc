# Fail on missing peer deps rather than silently degrading.
strict-peer-dependencies=true
# Reproducible installs: pin exact versions on `pnpm add`.
save-exact=true
# Keep the store link strategy explicit for cross-machine consistency.
link-workspace-packages=true
